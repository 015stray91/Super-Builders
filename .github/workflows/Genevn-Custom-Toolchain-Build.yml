- name: Compile Genevn Kernel
        run: |
          cd kernel-source
          
          # 1. KernelSU Setup
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -s main
          
          # 2. Set Environment Variables
          # Keeping these exactly as you need them for the build script to pick up your fragment
          export ARCH=arm64
          export SUBARCH=arm64
          export VARIANT=gki
          export TARGET_PRODUCT=parrot_genevn
          export MSM_ARCH=parrot
          
          # 3. Build using your verified toolchain and the moto build system
          # This triggers the ./build.sh which merges your moto-parrot-genevn.config fragment
          ./build.sh

      - name: Package Kernel via AnyKernel3
        run: |
          cd $GITHUB_WORKSPACE
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git AnyKernel
          
          # Copy the binary from the GKI output directory
          cp kernel-source/out/android12-5.10/dist/Image.gz* AnyKernel/ 2>/dev/null || \
          cp kernel-source/out/dist/Image.gz* AnyKernel/ 2>/dev/null
          
          # Apply your custom anykernel.sh from your repo
          cp .github/workflows/anykernel3/anykernel3.sh AnyKernel/anykernel.sh
          
          # Zip it up
          cd AnyKernel
          zip -r9 ../Genevn-KernelSU-Next-Flashable.zip *
