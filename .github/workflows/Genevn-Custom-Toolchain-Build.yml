youname: Genevn Custom Toolchain Build

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
    inputs:
      ksu_variant:
        description: "KernelSU variant"
        type: choice
        options:
          - KernelSU-Next
          - SukiSU
          - ReSukiSU
          - WKSU
        default: "KernelSU-Next"
      add_susfs:
        description: "Add SUSFS"
        type: boolean
        default: true
      add_zeromount:
        description: "Add ZeroMount"
        type: boolean
        default: true

jobs:
  build:
    name: "Build Custom Kernel - Genevn"
    runs-on: ubuntu-latest
    
    steps:
      - name: Free Disk Space
        uses: endersonmenezes/free-disk-space@v3
        with:
          remove_android: true
          remove_tool_cache: true

      - name: Checkout Super-Builders
        uses: actions/checkout@v5
        with:
          repository: 015stray91/Super-Builders
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Initialize Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y clang lld llvm gcc-aarch64-linux-gnu binutils bc bison flex libssl-dev libelf-dev cpio kmod python3 zip

      - name: Compile Genevn Kernel
        run: |
          cd kernel-source
          
          # 1. KernelSU Setup
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -s main
          
          # 2. INITIALIZE TOOLS & SCRIPTS DIR
          # This is the step you requested: we ensure the tools are in the build path.
          export KERNEL_DIR=$(pwd)
          export PATH=${KERNEL_DIR}/tools:$PATH
          
          # 3. SET FRAGMENT MERGER VARIABLES
          # 'TARGET_PRODUCT' triggers the fragment logic (add_moto_defconfig_fragments)
          # which looks for 'moto-parrot-genevn.config' and merges it automatically.
          export ARCH=arm64
          export VARIANT=gki
          export TARGET_PRODUCT=parrot_genevn
          export MSM_ARCH=parrot
          
          # 4. RUN MOTOROLA BUILDER
          # This script calls the binaries in /scripts/ to merge 
          # your fragments and drivers into the final Image.gz-dtb
          ./build.sh

      - name: Package Kernel via AnyKernel3
        run: |
          cd $GITHUB_WORKSPACE
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git AnyKernel
          
          # Copy binary from dist/ (GKI standard output)
          # We use a wildcard to ensure we grab the right image
          cp kernel-source/out/android12-5.10/dist/Image.gz* AnyKernel/ 2>/dev/null || \
          cp kernel-source/out/dist/Image.gz* AnyKernel/ 2>/dev/null
          
          # Copy your custom anykernel.sh from your repo
          cp .github/workflows/anykernel3/anykernel3.sh AnyKernel/anykernel.sh
          
          # Create Flashable Zip
          cd AnyKernel
          zip -r9 ../Genevn-KernelSU-Next-Flashable.zip *

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Genevn-Next-Kernel-Signed
          path: Genevn-KernelSU-Next-Flashable.zip
